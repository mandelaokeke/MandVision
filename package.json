{
    "name": "event-driven-media-pipeline",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "apps/*",
        "infra",
        "packages/*"
    ],
    "scripts": {
        "dev:web": "npm run dev -w apps/web",
        "build:web": "npm run build -w apps/web",
        "cdk": "npm run cdk -w infra",
        "deploy": "npm run deploy -w infra"
    },
    "dependencies": {
        "@aws-sdk/client-dynamodb": "^3.1084.0",
        "@aws-sdk/client-rekognition": "^3.1084.0",
        "@aws-sdk/client-secrets-manager": "^3.1084.0",
        "@aws-sdk/lib-dynamodb": "^3.1084.0"
    }
}
